services:
  postgres:
    image: postgres:17
    healthcheck:
      test: pg_isready -U admin -d fakecap
      interval: 5s
      timeout: 3s
      retries: 3
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: fakecap
